export const metadata = {
  title: "Blog & News",
  description: "Latest news, tips and insights from FindWithHabi — Nigeria's trusted property platform.",
}

export default function BlogLayout({ children }) {
  return children
}